# HRM System - Human Resources Management

Веб-додаток для управління персоналом з модулями для управління співробітниками, департаментами, вакансіями, навчанням, оцінкою продуктивності та обліком часу.

## 🚀 Швидкий старт

### Автоматичне розгортання

Найпростіший спосіб розгорнути проект - використати скрипт автоматичного розгортання:

```bash
chmod +x instructions/deploy.sh
./instructions/deploy.sh
```

Скрипт автоматично:
- Перевірить наявність необхідних залежностей
- Створить базу даних PostgreSQL
- Встановить залежності для backend та frontend
- Застосує схему бази даних
- Заповнить базу даних початковими даними (seed)

### Ручне розгортання

#### 1. Встановлення залежностей

**Backend:**
```bash
cd backend
npm install
cp .env.example .env  # Налаштуйте .env файл
npx prisma generate
npx prisma db push
npm run prisma:seed
```

**Frontend:**
```bash
cd frontend
npm install
```

#### 2. Налаштування бази даних

Створіть базу даних PostgreSQL:
```bash
createdb hrm_local
# або через psql:
psql -U postgres -c "CREATE DATABASE hrm_local;"
```

Налаштуйте `DATABASE_URL` в `backend/.env`:
```
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/hrm_local?schema=public"
```

#### 3. Запуск

**Backend:**
```bash
cd backend
npm run dev
```

**Frontend:**
```bash
cd frontend
npm run dev
```

Або використайте скрипт для одночасного запуску:
```bash
./run-local.sh
```

## 📋 Системні вимоги

- **Node.js**: версія 18.0.0 або вища
- **PostgreSQL**: версія 14.0 або вища
- **npm**: версія 9.0.0 або вища

## 🔐 Тестові облікові записи

Після виконання seed скрипту будуть створені наступні тестові користувачі:

- **Admin**: `admin@local.test` / `Admin@123`
- **HR**: `hr@local.test` / `Hr@12345`
- **Employee**: `employee@local.test` / `Emp@12345`
- **Candidate**: `candidate@local.test` / `Cand@12345`

## 📚 Документація

Детальна технічна документація знаходиться в папці `instructions/`:

- **resources.md** - Повний опис технологій, архітектури та структури проекту
- **deploy.sh** - Скрипт автоматичного розгортання

## 🏗️ Архітектура

Проект складається з двох основних частин:

- **Backend** (`/backend`) - Node.js + Express + Prisma + PostgreSQL
- **Frontend** (`/frontend`) - React + TypeScript + Material-UI

## 📦 Основні модулі

- **Управління співробітниками** - CRUD операції, профілі, документи
- **Департаменти** - Ієрархічна структура організації
- **Вакансії та рекрутинг** - Публічні вакансії, кандидати, резюме
- **Навчання** - Курси, записи, прогрес
- **360 Feedback** - Оцінка продуктивності, циклі оцінювання
- **Облік часу** - Відстеження робочого часу, відпустки

## 🔧 Технології

### Backend
- Node.js + Express
- TypeScript
- Prisma ORM
- PostgreSQL
- JWT авторизація
- Multer для завантаження файлів
- pdf-parse для парсингу резюме

### Frontend
- React 18
- TypeScript
- Material-UI (MUI)
- React Query для кешування даних
- Redux Toolkit для стану
- React Router для навігації

## 📝 API Документація

Після запуску backend, API документація доступна за адресою:
```
http://localhost:5050/api-docs
```

## 🧪 Тестування

```bash
# Backend тести
cd backend
npm test

# Frontend тести
cd frontend
npm test
```

## 📄 Ліцензія

Цей проект створено для навчальних цілей.

## 👤 Автор

Створено в рамках навчального проекту.

---

**Примітка**: Для детальної інформації про технології та розгортання дивіться `instructions/resources.md`

